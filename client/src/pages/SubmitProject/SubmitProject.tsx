import { Layout } from '../../components/Layout';

// - project name
// - optional description (technology used, interesting details)
// - team members (populated list of usernames from the db to choose from)
//   - team lead is assumed to be the current signed-in user
// - link to (github) repo
// - optional link to hosted project itself

export function SubmitProject() {
  return <Layout>Submit Project</Layout>;
}
